spring:
    application:
        name: sys
    cloud:
        nacos:
            server-addr: ty:8002
            config:
                shared-configs[0]:
                    data-id: common.yaml # 配置文件名-Data Id
                file-extension: yaml
server:
    port: 8101

